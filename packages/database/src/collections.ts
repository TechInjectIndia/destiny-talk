/**
 * Firestore collection names enum
 * Centralized collection name constants to prevent typos and ensure consistency
 */
export enum FirestoreCollections {
  USERS = 'users',
  REPORTS = 'reports',
  WALLET = 'wallet',
  WALLET_TRANSACTIONS = 'walletTransactions',
  ORDERS = 'orders',
  CHAT_MESSAGES = 'chatMessages',
  PROMPTS = 'prompts',
  ANALYTICS = 'analytics',
}

